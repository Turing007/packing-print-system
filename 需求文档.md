# 自动装箱打印系统 — 需求文档（完整版）

---

## 1. 项目概述

自动装箱打印系统是一款 Windows 桌面应用程序（Tkinter GUI），用于管理产品信息、进行装箱作业计算、生成并打印 10×10cm 箱响标签。所有数据以 JSON 文件存储在 data/ 目录中。

### 1.1 技术栈
- 语言: Python 3.12
- GUI: Tkinter（ttk）
- 打包: PyInstaller（单文件 EXE）
- 数据存储: 本地 JSON（5个）
- 打印方式: 生成 HTML → 浏览器打开 → Ctrl+P 打印

---

## 2. 模块架构

main.py —→ product.py —→ products.json
main.py —→ packing.py —→ product.py
main.py —→ storage.py —→ 4个 JSON

模块关系：main.py 依赖其他所有模块，packing.py 依赖 product.py（使用 Product 数据类），product.py 和 storage.py 各自独立，无循环依赖。

---

## 3. 模块详解

### 3.1 product.py - 产品管理模块

Product 数据类：name, sku, quantity, qty_per_box, spec, recipient, order_no
派生属性：full_boxes = quantity // qty_per_box, remainder = quantity % qty_per_box
持久化：to_dict() 只保存 name, sku, qty_per_box, spec

ProductStore：add, update, remove, get, list_all, count, search_by_name

### 3.2 packing.py - 装箱计算引擎

BoxLabel（@dataclass）: box_number, total_boxes, product_name, product_sku, qty_per_box, quantity_in_box, is_tail, recipient, order_no, spec, remark, created_at

calculate_boxes(products, merge_tail) -> list[BoxLabel]:
1. 计算 full_boxes 和 remainder
2. 不合并: 每个余数单独生成尾数箱
3. 合并: 按(recipient, order_no)分组，同组尾数合并
4. 重新编号

箱响布局：标题 -> 收件人(18pt) -> 订单号 -> 箱号/总箱数(22pt) -> 表格(SKU|品名|规格|装箱数|总数量) -> 页脚

箱响尺寸: 100mm x 100mm，每张一页
装箱数 = box.quantity_in_box（本箱实际数量）
总数量 = 按(product_name, order_no)聚合的合计值

### 3.3 storage.py - 数据存储模块

save_packing, load_current_packing, clear_current_packing - 装箱暂存
get_packing_history, delete_history_record - 历史记录
save_print_log, get_print_history, get_combined_history - 打印日志
save_order_to_history, load_order_history, delete_order_history - 订单历史

### 3.4 main.py - 主程序（PackingApp类）

产品管理: refresh_product_table, on_search_change, on_product_select, get_form_data
add_product, update_product, delete_product, clear_form

装箱作业: refresh_packing_table, on_packing_select, add_packing_item
modify_packing_item, remove_packing_item, clear_packing_items, next_order
calculate_boxes, refresh_box_table, _on_recipient_order_change

预览/打印: preview_packing_list, print_box_labels, print_selected_boxes, _open_html

暂存管理: save_current, load_current

历史管理: refresh_history_table, view_history_detail, show_detail_window
_reprint_from_record, reprint_history, delete_history
refresh_order_history, reload_from_order_history, batch_calculate_from_history
delete_selected_order_history

---

## 4. 数据流与逻辑关系

核心数据流：
1. 产品管理（products.json）→ 选择产品
2. 填写收件人/订单号/数量 → 添加到装箱项目列表
3. 装箱项目列表 → 计算装箱 → 箱响结果
4. 箱响结果 → 打印/预览 → HTML → 浏览器
5. 装箱项目列表 → 保存 → order_history.json
6. 历史 → 重新装箱 → 装箱列表
7. 历史 → 批量计算 → 箱响结果（按订单分开）

关键逻辑：
- 收件人/订单号修改时同步更新所有装箱项
- 相同收件人+订单号保存时覆盖旧记录
- 尾数合并按(recipient, order_no)分组
- 总数量按(product_name, order_no)聚合，不同订单不混合
- 元组键必须保持一致（副本错误会导致总数量显示为0）

---

## 5. 数据文件清单

| 文件 | 用途 | 备注 |
|------|------|------|
| products.json | 产品管理库 | name, sku, spec, qty_per_box |
| order_history.json | 装箱历史 | 含收件人/订单号/产品明细 |
| packing_history.json | 暂存/打印历史 | 完整装箱数据，保留最近100条 |
| current_packing.json | 当前暂存 | 仅最近一次暂存 |
| print_log.json | 打印日志 | 关联 packing_history.json |

---

## 6. 注意事项

- 打印时需要系统打印机设置自定义纸张 100mm x 100mm
- 数据存储在 EXE 同目录的 data/ 子目录，更新程序时保留 data/ 目录不丢失数据
- 产品管理的清空按钮仅清空编辑表单，不影响已保存的产品
- 装箱作业的清空按钮会清空装箱列表、收件人、订单编号
- 箱响预览 HTML 写入 %TEMP%/packing_labels/ 目录
- 一次最多级 500 个箱响（受浏览器打印性能影响）
- 元组键必须保持一致：存取和查询用相同的键（如(product_name, order_no)）
- 装箱数 = 本箱实际数量（quantity_in_box）
- 总数量 = 同一产品在同一订单中的所有箱的数量之和

---

## 7. 打包说明

pyinstaller --onefile --windowed --name "装箱打印系统" 
    --add-data "packing_label.css;." 
    --hidden-import tkinter --hidden-import tkinter.ttk 
    --hidden-import tkinter.messagebox main.py

输出: dist/装箱打印系统.exe

*文档版本: 2026-07-11*
