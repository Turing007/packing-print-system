# 装箱打印程序开发文档

## 1. 项目定位

本项目是一个 Windows 桌面端的装箱打印工具，基于 Python + Tkinter 实现，主要用于：

1. 维护产品资料。
2. 录入装箱任务。
3. 按装箱规则自动计算箱唛。
4. 生成可打印的 HTML 标签页面。
5. 保存装箱历史、打印日志和订单历史。

项目的核心能力不是简单的“按数量分箱”，而是一个带有尾数处理、尾数合并、订单维度编号、以及打印展示字段聚合的装箱计算引擎。

## 2. 技术栈

- Python 3.12
- Tkinter / ttk
- JSON 本地持久化
- HTML + CSS 标签预览与打印
- PyInstaller 打包为 Windows EXE

## 3. 目录与模块职责

### 3.1 主要文件

- `main.py`：主界面和业务编排入口。
- `product.py`：产品数据模型、装箱数解析、产品本地存储。
- `packing.py`：装箱算法、箱唛数据结构、HTML 生成。
- `storage.py`：装箱暂存、历史记录、打印日志、订单历史。
- `test_*.py`：单元测试。

### 3.2 模块依赖关系

```text
main.py -> product.py
main.py -> packing.py
main.py -> storage.py
packing.py -> product.py
```

模块边界比较清晰：

- `product.py` 只负责产品和装箱数定义，不关心界面。
- `packing.py` 只负责装箱结果生成，不直接操作 UI。
- `storage.py` 只负责 JSON 文件读写。
- `main.py` 负责把这些能力串起来。

## 4. 数据模型

### 4.1 Product

`Product` 是装箱计算的输入对象，包含以下字段：

- `name`：品名
- `sku`：SKU
- `quantity`：总数量
- `qty_per_box`：每箱装箱数，可支持多个候选值
- `spec`：规格
- `recipient`：收件人
- `order_no`：订单号
- `remark`：备注

其中 `qty_per_box` 是本项目最重要的输入之一，它既支持单值，也支持多个候选值，例如 `10,15,20`。

### 4.2 BoxLabel

`BoxLabel` 是装箱计算的输出对象，代表一张箱唛标签。主要字段包括：

- `box_number`：当前箱号
- `total_boxes`：总箱数
- `product_name` / `product_sku`
- `qty_per_box`：该箱对应的装箱容量
- `quantity_in_box`：本箱实际数量
- `is_tail`：是否尾数箱
- `recipient` / `order_no`
- `spec`
- `remark`
- `tail_items`：合箱时记录尾数拆分明细
- `product_remark` / `order_remark`

### 4.3 PackingRecord

`PackingRecord` 用于保存一次装箱过程的快照，适合暂存和历史回放：

- `products`：原始产品输入
- `boxes`：计算后的箱唛列表
- `merge_tail`：是否启用尾数合并
- `remark`：附加说明

## 5. 装箱算法

这是项目的核心。

### 5.1 算法目标

对每个产品，生成一组箱唛，满足以下目标：

1. 尽量减少箱数。
2. 优先使用较大的标准箱容量。
3. 尾数箱放在最后。
4. 支持多种装箱容量输入。
5. 计算结果要能直接用于打印。

### 5.2 装箱数解析

`qty_per_box` 通过 `parse_box_sizes()` 解析，支持：

- 数值：`10`
- 字符串：`10,15,20`
- 中文或其他分隔符：`10，15、20`
- 列表 / 元组 / 集合

解析后的结果会：

1. 过滤空值。
2. 校验必须是正整数。
3. 去重。
4. 按从大到小排序。

这个设计的意义是：同一产品可以同时存在多个可接受的单箱容量，算法会据此寻找更优的分箱方案。

### 5.3 `_packing_plan()` 的作用

`_packing_plan(quantity, box_sizes)` 负责为单个产品计算具体分箱方案。

它返回的结果可以理解为：

```text
[(本箱实际数量, 箱容量, 是否尾数), ...]
```

例如：

- 数量 `53`
- 装箱数 `10,15,20`

可能得到：

```text
[(20, 20, False), (20, 20, False), (13, 20, True)]
```

这表示：

- 2 个 20 件整箱
- 1 个 13 件尾数箱

### 5.4 算法策略

算法不是简单地做 `quantity // box_size`，而是会在多个容量之间寻找更优方案。

核心策略：

1. 优先寻找可组成的最少箱数方案。
2. 在能组成相同箱数时，优先让前面的箱子尽量满。
3. 如果无法整除，则保留最后一个尾数箱。

这意味着对于 `45` 和容量 `10,15,20`，结果会偏向：

- `20 + 15 + 10`

而不是：

- `15 + 15 + 15`

因为前者更符合“优先使用大箱”的业务习惯。

### 5.5 `calculate_boxes()` 的整体流程

`calculate_boxes(products, merge_tail, order_remark)` 是对外主入口。

#### 不合并尾数时

1. 按产品逐条计算分箱方案。
2. 每个产品独立生成箱唛。
3. 尾数箱直接保留为单独箱。
4. 重新编号。

#### 合并尾数时

1. 先按 `(recipient, order_no)` 分组。
2. 同组内计算每个产品的整箱与尾数。
3. 整箱仍按产品独立输出。
4. 尾数箱在同一收件人 + 同一订单号维度内合并。
5. 合并后的尾数箱重新编号。

### 5.6 合并尾数的规则

尾数合并是项目里另一个重要业务规则。

合并条件：

- 必须是尾数箱。
- 必须属于同一个收件人。
- 必须属于同一个订单号。

合并后会生成一个新的尾数箱，保存：

- 合并后的品名拼接文本
- 合并后的 SKU 拼接文本
- `tail_items` 明细
- 合并容量 `qty_per_box`

这样做的好处是打印时可以把多个尾数集中到一个箱唛上，减少零散尾数箱的数量。

### 5.7 箱号重新编号

计算完成后会调用重编号逻辑，确保：

- 同一个订单内箱号从 1 开始。
- `total_boxes` 在同一订单内保持一致。
- 打印展示时不会出现跳号。

这是因为尾数合并和手动合箱都可能改变最终箱列表，编号不能依赖中间计算过程，必须在最终结果上统一重算。

## 6. 箱唛打印逻辑

装箱结果最终会渲染为 HTML 页面，再由浏览器打印。

### 6.1 输出内容

每张箱唛页面一般包含：

- 标题
- 收件人
- 订单号
- 箱号 / 总箱数
- 表格内容：SKU、品名、规格、装箱数、总数量
- 页脚说明

### 6.2 打印尺寸

默认是 `100mm x 100mm`，一张标签对应一页。

### 6.3 字段含义

- `装箱数`：当前箱实际装了多少，即 `quantity_in_box`
- `总数量`：同一产品在同一订单维度下的合计数量

这两个字段不要混淆。前者用于描述单箱内容，后者用于描述整单汇总。

## 7. 数据存储

所有数据都落在本地 JSON 文件中，便于部署和离线使用。

### 7.1 主要数据文件

- `products.json`：产品资料。
- `order_history.json`：订单历史。
- `packing_history.json`：装箱历史。
- `current_packing.json`：当前暂存。
- `print_log.json`：打印日志。

### 7.2 存储原则

1. 所有文件使用 UTF-8。
2. 读写失败时尽量回退到空数据，避免程序启动失败。
3. 历史数据保留最近记录，避免文件无限增长。

## 8. 主界面职责

`main.py` 中的 `PackingApp` 负责以下几类交互：

### 8.1 产品管理

- 增加产品
- 修改产品
- 删除产品
- 搜索产品
- 清空表单

### 8.2 装箱作业

- 录入收件人、订单号、数量
- 将产品加入装箱列表
- 修改和删除装箱项目
- 执行装箱计算
- 预览 / 打印箱唛

### 8.3 历史管理

- 查看装箱历史
- 重新装箱
- 重新打印
- 删除历史记录
- 从订单历史批量计算

## 9. 单元测试覆盖点

当前测试主要围绕装箱算法，覆盖了这些重点：

1. 多装箱容量下的最少箱数方案。
2. 尾数的生成与排序。
3. 尾数合并逻辑。
4. 不同订单之间的编号隔离。
5. 手动合箱的合法性校验。
6. `parse_box_sizes()` 和 `format_box_sizes()` 的兼容性。

这说明项目的稳定性核心，不在 UI 细节，而在装箱规则是否始终一致。

## 10. 开发时最容易踩的坑

### 10.1 装箱数键值不一致

装箱结果的统计依赖元组键，如果存取时键结构不一致，就会出现总数量显示错误，甚至为 0。

### 10.2 尾数与整箱混算

尾数箱和整箱在统计时必须区分处理，否则会导致箱数、总数量和打印结果错位。

### 10.3 合并尾数的边界

尾数合并必须严格限制在同收件人、同订单号，否则业务语义会被破坏。

### 10.4 编号必须在最终结果上重算

不能在中间过程固定箱号，否则一旦发生合并或删除，编号会失真。

## 11. 运行与打包

### 11.1 本地运行

项目提供了批处理脚本用于启动。

### 11.2 打包

项目使用 PyInstaller 打包为单文件 EXE，打包后数据仍应放在 EXE 同目录的 `data/` 下，以便保留业务数据。

## 12. 建议的后续改进

如果后续继续维护，这几个方向最值得优先做：

1. 把装箱算法进一步拆成“计划生成”和“结果渲染”两个更小的层。
2. 为复杂的尾数合并补更细的回归测试。
3. 给装箱结果增加可导出的调试视图，方便人工核对。
4. 把数据读写统一封装，减少 `main.py` 的职责压力。

---

文档版本：2026-08-06
